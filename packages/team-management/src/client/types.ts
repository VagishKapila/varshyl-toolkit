/**
 * Client-side config passed when mounting team-management UI components.
 */
export interface ClientModuleConfig {
  /** Base API path where the server module is mounted. E.g. "/api/team" */
  apiBasePath: string;
}
