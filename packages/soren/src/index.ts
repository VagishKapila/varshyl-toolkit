/**
 * @varshylinc/soren — core wire contracts.
 *
 * Framework-agnostic, browser-safe types shared by the engine bridge
 * (`@varshylinc/soren/server`), the example adapter (`/reference`) and the
 * card UI (`/react`). No Node or DOM side effects.
 */
export * from './verbs';
export * from './cards';
export * from './pagination';
export * from './envelope';
export * from './display';
